<a class="logo" href="{{@site.url}}">
    {{#if @site.logo}}
        <img width="100" class="logo-image" src="{{@site.logo}}" alt="{{@site.title}}">
    {{else}}
        <span class="logo-text">{{@site.title}}</span>
    {{/if}}
</a>